<%= form_for([:admin, @staff]) do |f| %>
  <% if @staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@staff.errors.count, "error") %> prohibited this staff from being saved:</h2>

      <ul>
      <% @staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :designation %><br>
    <%= f.text_field :designation%>
  </div>
  <div class="field">
    <%= f.label :services %><br>
    <%= f.text_field :services %>
  </div>
   <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<div>
  <br>
  <%= link_to 'back', :back %>
</div>


  <script type="text/javascript">
    $(document).ready(function () {
      $("#staff_services").tokenInput("/services/search", { prePopulate: <%= raw @staff.services.to_json %> });
    });
  </script>