<%= form_for [:admin, @back_ground], :html => { :multipart => true } do |f| %>
  <div class='field'>
    <%= f.file_field :image %>
  </div>
  <div class='actions'>
    <%= f.submit %>
  </div>
<% end %>