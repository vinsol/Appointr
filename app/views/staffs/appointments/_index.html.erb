<table class = 'table table-hover table-striped'>
  <tr>
    <th>
      Customer
    </th>
    <th>
      Service
    </th>
    <th>
      Time
    </th>
    <th>
      Duration
    </th>
    <th>
      Date
    </th>
  </tr>
  <% @appointments.each do |appointment| %>
    <tr class='appointment <%= appointment.state %>' data-date='<%= appointment.start_at.to_date %>'> 
      <td>
        <%= appointment.customer.name.titleize %>
      </td>
      <td>
        <%= appointment.service.name.titleize %>
      </td>
      <td>
        <%= appointment.start_at.strftime("%I:%M %p") %>
      </td>
      <td>
        <%= "#{ appointment.duration } minutes" %>
      </td>
      <td>
        <%= appointment.start_at.strftime("%d %b %Y") %>
      </td>
    </tr>
  <% end %>
</table>
<br />
